# MainStreet AI — Website

Hand-built static site. Edit pages in src/content/, run `node build.js`, push.
Fill CONTACT_WEBHOOK in js/main.js with the n8n production webhook at launch.
Point a custom domain (e.g. mainstreetai.com/.org) at GitHub Pages when ready;
links are relative so both the github.io staging URL and a root domain work.
